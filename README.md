# Repositório para armazenamento dos Protobuf para POC de Data Persistence

## Instalar `protoc`

### Mac
``` bash
brew install protoc
```

## Usando pela primeira vez

``` bash
protoc --java_out=java protobuf/*.proto
```
